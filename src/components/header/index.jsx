import React from 'react';
import PropTypes from 'prop-types';

header.propTypes = {
    
};

function header(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default header;